server:
  port: 5003
  
spring:
  application:
    name: SUBMISSION-SERVICE
    
  datasource:
    url: jdbc:mysql://localhost:3306/yt_task_submission_service
    username: root
    password: cscorner
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

    
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true            
    register-with-eureka: true
    serviceUrl:                   
      defaultZone: http://localhost:8070/eureka/

    